# rigctld@.service — parameterised rigctld instance
[Unit]
Description=Hamlib rig control daemon (model %i)
After=network.target
[Service]
Type=simple
ExecStart=/usr/local/bin/start-rigctld %i
Restart=on-failure
RestartSec=3
User={{ radio_user }}
[Install]
WantedBy=multi-user.target
